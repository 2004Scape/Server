export enum ChatModePublic {
    ON = 0,
    FRIENDS = 1,
    OFF = 2,
    HIDE = 3,
}

export enum ChatModePrivate {
    ON = 0,
    FRIENDS = 1,
    OFF = 2,
}

export enum ChatModeTradeDuel {
    ON = 0,
    FRIENDS = 1,
    OFF = 2,
}
