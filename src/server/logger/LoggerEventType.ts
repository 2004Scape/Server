enum LoggerEventType {
    // server engine only
    ENGINE = 0,
    // wealth_log
    WEALTH,
    // session_log
    MODERATOR,
    ADVENTURE, // visible to players!
}

export default LoggerEventType;
