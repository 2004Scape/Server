[oploc1,loc_2054]
switch_int(%elena_progress) {
    case ^elena_complete: ~open_and_close_door(loc_param(next_loc_stage), loc_coord, loc_angle, loc_shape, ~check_axis(coord, loc_coord, loc_angle), false);
    case default: mes("The door is locked.");
}
