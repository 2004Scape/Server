[opnpc1,wizard_mizgog]
~chatnpc("default", "Hello there");

if(%imp_progress = 0) {
    def_int $options = ~p_choice2("Give me a quest!", 1, "Most of your friends are pretty quiet aren't they?", 2);

    if($option = 1) {
        @imp_catcher_start;
    } else if($option = 0) {

    }
} else if(%imp_progress = 1) {
    @imp_catcher_inprogress;
} else if(%imp_progress = 2) {
    ~chatplayer();
}