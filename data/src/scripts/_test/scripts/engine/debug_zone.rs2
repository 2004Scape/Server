[debugproc,zone]
def_int $x = coordx(coord);
def_int $z = coordz(coord);

def_int $zoneX = divide($x, pow(2, 3));
def_int $zoneZ = divide($z, pow(2, 3));

mes("Zone: <tostring($zoneX)>_<tostring($zoneZ)>");
