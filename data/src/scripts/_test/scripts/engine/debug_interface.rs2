[debugproc,open](interface $interface)
if (map_localdev = false | $interface = null) {
    return;
}

if_openmainmodal($interface);

[debugproc,close]
if (map_localdev = false) {
    return;
}

if_close;
