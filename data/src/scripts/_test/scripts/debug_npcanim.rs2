[debugproc,npcanim]
npc_findallzone(coord);
while (npc_findnext = true) {
    npc_anim(ogre_attack, 0);
}

[debugproc,npcanim2]
npc_findallzone(coord);
while (npc_findnext = true) {
    npc_anim(ogre_block, 0);
}

[debugproc,npcanim3]
npc_findallzone(coord);
while (npc_findnext = true) {
    npc_anim(ogre_death, 0);
}
