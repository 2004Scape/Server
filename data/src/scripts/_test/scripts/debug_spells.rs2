[debugproc,giverunes]
inv_add(inv, airrune, 1000);
inv_add(inv, waterrune, 1000);
inv_add(inv, earthrune, 1000);
inv_add(inv, firerune, 1000);
inv_add(inv, mindrune, 1000);
inv_add(inv, bodyrune, 1000);
inv_add(inv, deathrune, 1000);
inv_add(inv, chaosrune, 1000);
inv_add(inv, lawrune, 1000);
inv_add(inv, naturerune, 1000);
inv_add(inv, cosmicrune, 1000);
inv_add(inv, bloodrune, 1000);
inv_add(inv, soulrune, 1000);

[debugproc,superheat]
inv_add(inv, iron_ore, 1);
@magic_spell_superheat(magic:com_21, iron_ore);

[debugproc,lowalch]
def_namedobj $item = iron_ore;
inv_add(inv, $item, 1);
@magic_spell_low_alch(magic:com_10, $item);

[debugproc,highalch]
def_namedobj $item = iron_ore;
inv_add(inv, $item, 1);
@magic_spell_high_alch(magic:com_26, $item);

[debugproc,chargeorb]
def_loc $loc = loc_2150;
@magic_spell_charge_orb(magic:com_30, $loc);

[debugproc,enchant]

@magic_spell_enchant(magic:com_35, 1);