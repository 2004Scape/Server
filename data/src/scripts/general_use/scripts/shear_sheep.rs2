[opnpcu,sheep_fluffy]
if(last_item ! shears) {
    @nothing_interesting_happens;
}
anim(human_shearing, 0);
p_delay(0);
npc_facesquare(coord);
//npc_delay(2) TODO: uncomment 
p_delay(1);
if(inv_freespace(inv) = 0) {
    obj_add(coord, wool, 1, 200);
} else {
    inv_add(inv, wool, 1);
}
// TODO: NPC transform to sheared sheep
mes("You get some wool.");
npc_say("Baa!");
~objbox(wool, 250, "You get some wool.");