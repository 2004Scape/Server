[ai_timer,drake]
npc_settimer(calc(15 + random(20)));
if (random(4) = 0) {
    @duck_quack;
}

[ai_timer,duck]
npc_settimer(calc(15 + random(20)));
if (random(4) = 0) {
    @duck_quack;
}

[ai_timer,duckling]
npc_settimer(calc(15 + random(20)));
if (random(4) = 0) {
    npc_say("Eep!");
}

[label,duck_quack]
if (random(2) = 0) {
    npc_say("Quack!");
} else {
    npc_say("Quack?");
}
