[proc,npc_death]
npc_anim(npc_param(death_anim), 0);
if (finduid(%npc_retaliation_pid) = true) { // TODO
    sound_synth(npc_param(death_sound), 0, 0);
}
npc_delay(npc_param(deathanim_delay));
npc_del;
