[label,macro_event_prayer]
if (random(calc(~macro_event_general_count + 2)) < ~macro_event_general_count) {
    @macro_event_general_spawn;
}

if (random(2) = 0) { // 50% chance
    @macro_event_zombie_spawn;
} else {
    @macro_event_shade_spawn;
}