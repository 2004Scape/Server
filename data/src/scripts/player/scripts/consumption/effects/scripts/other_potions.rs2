// TODO: anti poison
[proc,consume_effect_antipoison]
sound_synth(liquid, 0, 0);
def_obj $consumable = last_item;
mes("Unimplimented");
~potion_effect_message($consumable);

// TODO: anti fire
[proc,consume_effect_antidragon]
sound_synth(liquid, 0, 0);
def_obj $consumable = last_item;
mes("Unimplimented");
~potion_effect_message($consumable);